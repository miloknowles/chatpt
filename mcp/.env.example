# MCP server Supabase config.
# You can also reuse NEXT_PUBLIC_* env vars, but dedicated MCP_* vars are preferred.
MCP_SUPABASE_URL=
MCP_SUPABASE_PUBLISHABLE_KEY=

# Optional fallback accepted by the server:
# MCP_SUPABASE_ANON_KEY=
